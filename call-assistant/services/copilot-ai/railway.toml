[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["services/copilot-ai/**"]

[deploy]
startCommand = "uvicorn src.api.main:app --host 0.0.0.0 --port 8080"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
