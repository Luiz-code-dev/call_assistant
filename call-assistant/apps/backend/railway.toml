[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["apps/backend/**", "apps/backend/Dockerfile"]

[deploy]
startCommand = "java -jar app.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
